[expression-calculator](expression-calculator)
ExpressionCalculator 数学计算和比较的接口。

String calculation +-*/() 计算表达式的值。
boolean compareCalculation 比较表达式的值，返回布尔值。

---

[task](task)
Task.1
参考目前的实现: 阅读下面的代码，理解它的功能和实现细节。 
[IterativeExpressionCalculator.java](expression-calculator/src/main/java/com/example/calculator/IterativeExpressionCalculator.java)
[RecursiveExpressionCalculator.java](expression-calculator/src/main/java/com/example/calculator/RecursiveExpressionCalculator.java)

Task.2
参考我提供的测试用例: 阅读测试用了。 

[ExpressionCalculatorBoundaryRegressionTest.java](expression-calculator/src/test/java/com/example/calculator/ExpressionCalculatorBoundaryRegressionTest.java)
[ExpressionCalculatorTest.java](expression-calculator/src/test/java/com/example/calculator/ExpressionCalculatorTest.java)
[IterativeExpressionCalculatorDeepNestingTest.java](expression-calculator/src/test/java/com/example/calculator/IterativeExpressionCalculatorDeepNestingTest.java)
[IterativeExpressionCalculatorSpecialTypesTest.java](expression-calculator/src/test/java/com/example/calculator/IterativeExpressionCalculatorSpecialTypesTest.java)


请先获取所有需要的权限，下面操作将自动允许， 任务处理过程种自动允许 下列操作。 
信任工作路径下全部操作： 读写执行，文件修改，代码修改等。 信任GIT权限，操作任何GIT之类。
信任 D:\application\java 信任所有读 执行操作。
信任 通过 D:\application\java 配置临时环境变量的操作。
工作路径下的所有操作， 包括读取文件，写入文件，执行代码等。 这将确保你能够顺利地完成任务，并且能够访问和修改相关的代码和测试用例。



Task.3
测试用例提供我最终想要的功能和边界条件。 你需要根据这些测试用例来实现 ExpressionCalculator 接口的功能。
请编写一份高质量的实现，确保所有测试用例都能通过。 
重新实现 IterativeExpressionCalculator 和 RecursiveExpressionCalculator 类，确保它们都能正确计算表达式的值，并且能够处理各种边界条件和特殊情况。

任务目标: 

1. 理解 ExpressionCalculator 接口的功能和实现细节。
2. 理解测试用例中定义的功能和边界条件。
3. calculation 支持 +-*/() 运算符的表达式计算。
4. compareCalculation：

```X
- 比较表达和运算： +-*/ () == != > < >= <= && ||, 所有数字类型处理默认均转换为 BigDecimal 进行计算，布尔值处理为 true/false。
- 支持比较两个表达式的值，返回布尔值。
- 例如，"2 + 3 == 5" 应该返回 true，
- 复杂的表达式比较，如 "2 * (3 + 4) > 10" 应该返回 true。
    "4 / 2 <= 2 - 1" 应该返回 true。
- 变量表达: 
    Map<String, Object> variables = new HashMap<>();
    variables.put("x", 5);
    variables.put("y", 10);
    variables.put("a", 10);
    String expression = "x + y > a + 1";
    boolean result = expressionCalculator.compareCalculation(expression, variables);

- 支持 && || 运算符的逻辑比较。
    String expression = "x > 0 && y < 20";
    boolean result = expressionCalculator.compareCalculation(expression, variables);

- 支持 层级比较，如 "x > 0 && (y < 20 || a == 10)"。
- 支持布尔值或布尔变量
    variables.put("isTrue", true);
    String expression = "isTrue && x > 0";
    boolean result = expressionCalculator.compareCalculation(expression, variables);
    String expression = "true"; // should return true
    String expression = "false"; // should return false
    String expression = "isTrue == true && x > 0";

- 处理边界条件和特殊情况，如除以零、无效的表达式、未定义的变量等。
- 文件单变量值： 
    variables.put("file", new File("test.txt"));
    String expression = "file"; // 文件存在 应该返回 true，文件不存在应该返回 false 值为 ： file!= null && file.exists()
    String expression = "file == null"; // 判断文件变量是否为 null， 文件变量null应该返回 true，非 null 应该返回 false
    Object result = expressionCalculator.calculation(expression, variables);
    
- 集合单变量值： 
    集合包含: Map, List, Set 等等
    variables.put("list", Arrays.asList(1, 2,3));
    String expression = "list"; // 集合非空应该返回 true，集合为空应该返回 false 值为： list != null && !list.isEmpty()
    String expression = "list == null"; // 判断集合变量是否为 null，集合变量null应该返回 true，非 null 应该返回 false
    String expression = "list != null"; // 判断集合变量是否非 null，集合变量null应该返回 false，非 null 应该返回 true
    Object result = expressionCalculator.calculation(expression, variables);  

- 变量方法调用：
    1 无参数方法调用：
    variables.put("str", "Hello World");
    String expression = "str.length()"; // 应该返回 11
    Object result = expressionCalculator.calculation(expression, variables);
    
    variables.put("file", new File("test.txt"));
    String expression = "file.exists()"; // 文件存在应该返回 true，文件不存在应该返回 false
    Object result = expressionCalculator.calculation(expression, variables);
    
    
    2 有参数方法调用：方法调用的参数来自于变量表。只要能找到方法参数的值，就可以调用方法。 方法传参时候的特殊数字类型,不再变量表种: 例如： 123 == int 类型， 123L == long 类型， 123.45 == double 类型， 123.45f == float 类型， 123.45d == double 类型， 123.45m == BigDecimal 类型, true / false == boolean类型。 方法参数类型的特殊数字类型默认转换为 BigDecimal 进行方法调用。
    variables.put("num1", new BigDecimal("123"));
    variables.put("num2", new BigDecimal("456"));
    String expression = "num1.add(num2)"; // 应该返回 BigDecimal("579")
    Object result = expressionCalculator.calculation(expression, variables);
    
    variables.put("num1", new BigDecimal("123"));
    variables.put("num2", 55); // 数字类型默认转换为 BigDecimal， 所以 num1 查找 add() 方法是 BigDecimal 的 add() 方法，
    String expression = "num1.add(num2)"; // 应该返回 BigDecimal("178")
    Object result = expressionCalculator.calculation(expression, variables);
    
```

注意： 
Map<String, Object> variables 是一个可选参数，用于在表达式中引用变量。 你需要在实现中处理这些变量，并确保它们能够正确地参与计算和比较。
Map.value: 所有的数字都作为 BigDecimal 进行处理，布尔值处理为 true/false，文件和集合的特殊处理如上所述。
(重点) 方法调用时的参数直接写的数字或布尔值使用特殊使用直接类型， 从Map变量表查询出来的类型， 数字类型默认转换为 BigDecimal 进行方法调用。
例如: 
variables.put("num1", new BigDecimal("123"));
variables.put("num2", 55); // 数字类型默认转换为 BigDecimal;

String expression = "num1.add(num2)"; // 应该返回 BigDecimal("178")， 因为 num1 是 BigDecimal 类型， num2 是数字类型，处理时 new BigDecimal("55") 进行方法调用。

String expression = "num1.add(55)"; // 异常， 直接参数是数字类型， BigDecimal 没有 add(int) 方法！ 没有方法的时候应该抛出异常，提示方法调用失败，参数类型不匹配。

variables.put("str","geekbang");
String expression = "str.substring(0, 4)"; // 应该返回 "geek", 因为 str 是 String 类型， substring(int, int) 方法参数是 int 类型， 直接参数 0 和 4 是int 类型， 处理时直接调用 str.substring(0, 4) 方法。
Object result = expressionCalculator.calculation(expression, variables);

---
方法直接传入数值参数:  
123 == int 类型， 
123L == long 类型， 
123.45 == double 类型，
123.45f == float 类型， 
123.45d == double 类型， 
123.45m == BigDecimal 类型,
true / false == boolean类型。
---


可用工具: 
BeanUtils
Map<String, List<Method>> findPublicMethods(Class<?> clazz); 返回 类下面所有公共方法的列表， 包括方法名和方法参数类型。 这个工具可以帮助我们在实现方法调用时，找到正确的方法进行调用。

/**
* 搜索匹配的Method，类型匹配: int  (int , Integer)均匹配
* @param clazz bean type
* @param methodName 方法名字
* @param parameterTypes 参数类型
* @return 查找方法
*/
public static Method findFuzzyMethod(Class<?> clazz, String methodName, Class<?>... parameterTypes); 这个工具可以帮助我们在实现方法调用时，找到正确的方法进行调用，支持模糊匹配参数类型，例如 int 和 Integer 都可以匹配。
---

为防止意外情况，请生成的代码时，确保代码质量高，注释清晰，并且能够正确处理各种边界条件和特殊情况。 你可以使用异常处理来捕获和处理可能出现的错误，例如无效的表达式、未定义的变量、除以零等情况。
防止出现中断情况，请记录每一步要做的事情，和写入日志，方便调试和排查问题。
防止业务逻辑错误，确保实现的功能符合预期，并且所有测试用例都能通过， 并增加一些测试用例。


TASK.4
你应该存在 两个 实现类： IterativeExpressionCalculator 和 RecursiveExpressionCalculator， 这两个类都应该实现 ExpressionCalculator 接口，并且它们的功能应该完全相同。 你需要确保这两个类都能够正确计算表达式的值，并且能够处理各种边界条件和特殊情况。
完成实现后，运行所有测试用例，确保它们都能通过。 如果有任何测试用例失败，仔细检查你的实现，找出问题所在，并进行修复.
测试用例分为3次测试， 
1. 使用现有用例测试， 
2. 生成新的测试用例测试， 新的测试用例包含所有逻辑情况（包含方法层面的调用， 变量引用）， 
3. 生成新的边界条件和特殊情况测试。 每次测试后都要进行代码质量检查，确保代码清晰、注释充分，并且能够正确处理各种边界条件和特殊情况。

每一次的测试通过之后，请进行GIT提交，防止后续变更意外情况导致代码丢失。 提交内容应该包含本次测试的结果，以及任何重要的修复或改进。 例如，提交信息可以是 "实现 IterativeExpressionCalculator 和 RecursiveExpressionCalculator 类，支持表达式计算和比较功能，并通过所有测试用例"。

测试完成后，请二次确认代码质量，确保代码清晰、注释充分，并且能够正确处理各种边界条件和特殊情况。 最后，提交你的代码实现，并提供一个简要的说明，描述你的实现思路和任何重要的设计决策。

以上操作的权限，均自动授权，自动确认。 